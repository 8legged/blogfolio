development:
  adapter: postgresql
  encoding: unicode
  database: blogfolio_<%= Rails.env %>
  # database: <%= Rails.env %>
  pool: 5
  username: postgres
  # username:
  min_messages: ERROR

test:
  adapter: postgresql
  encoding: unicode
  database: blogfolio_<%= Rails.env %>
  # database: <%= Rails.env %>
  pool: 5
  username: postgres
  # username:
  min_messages: ERROR

production:
  adapter: postgresql
  encoding: unicode
  database: blogfolio_<%= Rails.env %>
  # database: <%= Rails.env %>
  pool: 5
  username: postgres
  # username:
  min_messages: ERROR
